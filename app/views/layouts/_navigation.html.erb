<div class="ui small menu">
  <div class="ui container">
    <%= link_to "Chatroom", root_path, class:"item"%>
    <a class="item">
    Messages
    </a>
    <div class="right menu">
      <div class="ui dropdown item">
        Account <i class="dropdown icon"></i>
          <div class="menu">
            <% if user_signed_in? %>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "item" %>
            <%= link_to "Settings", edit_user_registration_path, class: "item" %>
            <% else %>
            <%= link_to "Log in", new_user_session_path, method: :post, class: "item" %>
            <%= link_to "Sign up", new_user_registration_path, class: "item" %>
            <% end %>
              </div>
            </div>
            <% if user_signed_in? %>
          <div class="item">
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "ui red button" %>
          </div>
            <% else %>
            <div class="item">
            <%= link_to "Log in", new_user_session_path, class: "ui primary button" %>
            </div>
      <% end %>
    </div>
  </div>
</div>
